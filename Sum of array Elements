using System;

class Program
{
    static void Main()
    {
        int[] numbers = { 2, 4, 6, 8 };
        int sum = 0;

        foreach (int n in numbers)
            sum += n;

        Console.WriteLine("Sum = " + sum);
    }
}
